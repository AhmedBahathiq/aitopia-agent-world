# Security

Please report suspected vulnerabilities privately through GitHub's **Report a vulnerability** feature instead of opening a public issue.

Never commit `.env`, `.dev.vars`, OpenAI keys, Cloudflare credentials, or the admin signing secret. Rotate a secret immediately if it is exposed.

Security fixes are prioritized for the latest version on `main`.
