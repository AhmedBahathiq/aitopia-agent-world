{
  "name": "@agent-world/engine",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ./wrangler.jsonc",
    "deploy": "wrangler deploy --config ./wrangler.jsonc",
    "types": "wrangler types --config ./wrangler.jsonc",
    "check": "tsc --noEmit",
    "security:static": "node ./scripts/check-agent-capabilities.mjs",
    "test": "npm run security:static && vitest run"
  },
  "dependencies": {
    "agents": "0.22.0",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260911.1",
    "typescript": "5.9.3",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  }
}
